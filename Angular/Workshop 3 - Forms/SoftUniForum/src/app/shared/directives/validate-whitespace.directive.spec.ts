import { ValidateWhitespaceDirective } from './validate-whitespace.directive';

describe('ValidateWhitespaceDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateWhitespaceDirective();
    expect(directive).toBeTruthy();
  });
});
